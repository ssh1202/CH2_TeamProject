// 파일명: Character.h
#pragma once
#include <iostream>
#include <string> // 문자열(이름)을 쓰기 위해 필요합니다.

using namespace std;

// [Mission 1] 클래스 선언
// 언리얼 표준: 클래스 이름은 'A'로 시작합니다.
class ACharacter
{
public:
    ACharacter();
    ~ACharacter();

    // ---------------------------------------------------------
    // [Mission 2] 멤버 변수 선언 (명사 사냥 결과물)
    // ---------------------------------------------------------
    // 이름, 체력, 공격력은 자식(Player, Monster)들이 물려받아 써야 합니다.
    // 따라서 'protected' 접근 지정자를 사용합니다.
protected:
    // Q1. 문자열(string) 변수 'Name'을 선언하세요. (이름)
    // (여기에 작성)

    // Q2. 정수형(int) 변수 'Hp'를 선언하세요. (체력)
    // (여기에 작성)

    // Q3. 정수형(int) 변수 'Atk'를 선언하세요. (공격력)
    // (여기에 작성)


    // ---------------------------------------------------------
    // [Mission 3] 멤버 함수 선언 (동사 사냥 결과물)
    // ---------------------------------------------------------
    // 행동은 외부에서 시키는 것이므로 'public'으로 선언합니다.
public:
    // Q4. 'Attack' 함수를 선언하세요. (반환값 void, 입력값 없음)
    // (여기에 작성)

    // Q5. 'TakeDamage' 함수를 선언하세요.
    // - 반환값: void
    // - 입력값: int DamageAmount (받을 데미지 양)
    // (여기에 작성)

};